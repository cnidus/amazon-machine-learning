version: 0.2

phases:
  install:
    commands:
      - echo nothing in the install step
  pre_build:
    commands:
      - npm install
      - npm install -g @angular/cli
  build:
    commands:
      - ng build
  post_build:
    commands:
      - echo Build completed on `date`
artifacts:
  files:
    - dist/*